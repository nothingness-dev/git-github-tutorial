Commands I learned:

git config --global,
git status, git add,
git commit -m "desc",
git log,
git log --oneline,
git branch,
git checkout,
git checkout -b,
git stash,
git status,
git stash list,
git stash pop,
git revert HEAD,
git reset --soft HEAD~1,
git reset -soft HEAD~1,
git rm --cached,
git push -u origin main,
git push -u origin branch name,
git fetch origin,
git log origin/main,
git pull origin main,
git diff,
git diff COMMIT1 COMMIT2,

Challenges faced:

diff and stash, especially stash command was somehow hard to understand.

Key takeaways:

i can push files on main branch or other branches and merge them into main branch.
i can reverse changes and see logs.
i can stash my commit and when i done i go back to my commit.

Questions:
no questions at this momment, thank you for this course.